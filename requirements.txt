streamlit==1.50.0

# TensorFlow + legacy checkpoint loading via tf-keras.
# Keep a compatible range so Streamlit Cloud can pick an available wheel
# for the configured Python version (see runtime.txt).
tensorflow>=2.16,<2.22
tf-keras>=2.16,<2.22

# Streamlit requires protobuf < 7; TF requires protobuf >= 6.31.1.
protobuf>=6.31.1,<7

# Runtime deps
opencv-python-headless==4.12.0.88
imageio==2.37.0
pillow>=10.0.0
gdown==6.0.0
